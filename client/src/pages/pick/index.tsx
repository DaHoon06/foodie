import {NextPage} from "next";
import {BasicLayout} from "@layouts/BasicLayout";
import {ReactElement} from "react";

const PickPage: NextPage = (): ReactElement => {
  return (
    <BasicLayout>
      <div>
        픽
      </div>
    </BasicLayout>
  )
}
export default PickPage;
